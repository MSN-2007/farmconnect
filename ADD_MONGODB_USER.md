# Fix Existing MongoDB Cluster - Create New User

## Quick Fix (3 minutes)

You have an active cluster, so we just need to create a new database user with the correct credentials.

### Step 1: Create New Database User

1. Go to https://cloud.mongodb.com
2. Click **"Database Access"** in the left sidebar
3. Click **"Add New Database User"**
4. Fill in:
   - **Authentication Method**: Password
   - **Username**: `farmconnect_admin` (or any name you prefer)
   - Click **"Autogenerate Secure Password"**
   - **COPY THE PASSWORD** immediately!
5. **Database User Privileges**:
   - Choose **"Built-in Role"**
   - Select **"Atlas admin"** (or "Read and write to any database")
6. Click **"Add User"**
7. Wait 1-2 minutes for user to be created

### Step 2: Verify Network Access

1. Click **"Network Access"** in left sidebar
2. Make sure you see: `0.0.0.0/0` (Allow access from anywhere)
3. If not, click **"Add IP Address"** → **"Allow Access from Anywhere"** → Confirm

### Step 3: Get Connection String

1. Go to **"Database"** in left sidebar
2. Click **"Connect"** button on your cluster
3. Choose **"Connect your application"**
4. Copy the connection string
5. Replace `<password>` with the password you copied
6. Add `/farm-connect` before the `?`

**Example:**
```
mongodb+srv://farmconnect_admin:YOUR_COPIED_PASSWORD@cluster0db01.tq75ums.mongodb.net/farm-connect?retryWrites=true&w=majority
```

### Step 4: Send Me the Connection String

Once you have it ready, send me the complete connection string and I'll update your `.env` file!

---

## Important Notes

- Use the **autogenerated password** - it's more secure
- Make sure to **copy the password** before closing the popup
- The connection string should have your cluster name: `cluster0db01.tq75ums.mongodb.net`
- Add `/farm-connect` to specify the database name

---

**Ready? Create the user and send me the connection string!**
